# Clean project fixture

Deliberately contains nothing that should trigger any check across any
category -- the false-positive baseline. If a change to any checker makes
this fixture produce a finding, that's a regression.
